import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const background = Color(0xFFF8F9FB);       // Main Background
  static const sidebarBg = Color(0xFFF8F8FA);         // Sidebar Background
  static const cardBg = Color(0xFFF1EAFD);            // Card Background

  static const white = Color(0xFFFFFFFF);

  // Text
  static const textPrimary = Color(0xFF1F2937);       // Heading text
  static const textSecondary = Color(0xFF9CA3AF);     // Subtitle / section label / inactive text

  // Purple Accents
  static const purple = Color(0xFF8B5CF6);            // Primary Purple
  static const purpleLight = Color(0xFFF1EAFE);       // Light Purple (active bg)

  // Utility
  static const divider = Color(0xFFEAEAEA);
  static const iconDefault = Color(0xFF90909A);
}
